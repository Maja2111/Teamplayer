//import { useState } from 'react'
import './App.css'

function App() {
  //  const [count, setCount] = useState(0)

  // AUFGABE 1

  let zufallszahlen = [];

  for (let i = 0; i < 11; i++ ) {
    let zufallszahlen[i] = Math.random() * (50 - 0) + 50
  }

  return (
    <>

    </>
  )
}

export default App
